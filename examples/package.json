{
  "name": "@kjn/svg-pan-zoom-examples",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel index.html",
    "build": "npm --prefix .. install && npm --prefix .. run prepublishOnly && parcel build index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@kjn/svg-pan-zoom": "file:.."
  },
  "devDependencies": {
    "parcel": "^2.5.0"
  }
}
